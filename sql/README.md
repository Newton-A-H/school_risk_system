The SQL files in this folder were originally written for SQL Server and are now legacy reference material.

The application currently targets PostgreSQL through SQLAlchemy and bootstraps the active schema from the ORM models.

If you need database scripts for PostgreSQL later, generate them from the current SQLAlchemy models or add PostgreSQL-specific migration files rather than reusing the old SQL Server scripts unchanged.
